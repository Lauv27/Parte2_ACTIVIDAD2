/**
* Esta clase define objetos que representan cuadrados con un lado como atributo.
* @autor Laura Rincon, Sebastian Chidiak
*/
public class Cuadrado {
    int lado; // Atributo que almacena la longitud de un lado de un cuadrado
    
    /**
    * Constructor de la clase Cuadrado.
    * @param lado Parámetro que se utiliza para definir la longitud de un lado de un cuadrado.
    */
    public Cuadrado(int lado) {
        this.lado = lado;
    }
    
    /**
    * Método que calcula el área de un cuadrado y devuelve el resultado.
    * El área se calcula elevando al cuadrado la longitud de un lado.
    * @return El área de un cuadrado.
    */
    double calcularArea() {
        return lado * lado;
    }
    
    /**
    * Método que calcula el perímetro de un cuadrado y devuelve el resultado.
    * El perímetro se calcula multiplicando la longitud de un lado por cuatro.
    * @return El perímetro de un cuadrado.
    */
    double calcularPerimetro() {
        return 4 * lado;
    }
}
